let mongoose,bcrypt;_443‍.x([["default",()=>_443‍.o]]);_443‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_443‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);

const {Schema} = mongoose;

const userSchema = new Schema({
    name: {
        type: String,
        trim: true,
        required: "Name is required"
    },
    email: {
        type: String,
        trim: true,
        required: "Email is required",
        unique: true
    },
    password: {
        type: String,
        required: true,
        min: 6,
        max: 20
    },
    stripe_account_id: "",
    stripe_seller: {},
    stripeSession:{}

},
{timestamps: true}
);

//"pre" middlewares
userSchema.pre("save", function(next){
    let user = this;
    if(user.isModified("password")){
        return bcrypt.hash(user.password, 12, function(err, hash){
            if(err){
                _443‍.g.console.log("BCRYPT ERR===> " + err)
                return next(err);
            }
            else{
                user.password = hash;
                return next();
            }
        })
    }
    else{
        return next();
    } 

});
//checking the password
userSchema.methods.comparePassword = async function(password, next){
    try{
        const result = await bcrypt.compare(password, this.password);
        return result;
    }catch(err){
         console.log(`Error when comparing passwords: ${err.message}`)
    }

}


_443‍.d(mongoose.model("User", userSchema));